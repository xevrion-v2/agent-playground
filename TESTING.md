# Testing

## Running Tests
```bash
npm run test --workspaces --if-present
```

## Writing Tests
- Use Vitest as the test runner
- Place tests in `__tests__/` directories
- Follow existing patterns for route/service tests
