 and syntax):
```python
from leaderboard import update_leaderboard
from unittest.mock import patch

# Mock database connection for tests
class SQLiteDatabase:
    def __init__(self):
        self.contributors = {}
    
    def add_contributor(self, name, score):
        self.contributors[name] = score
    
    def get_contributor(self, name):
        return self.contributors.get(name)
    
    def update_contributor(self, name, score):
        self.contributors[name] = score
```

--- 
// FILE: .github/pull_request_template.md
---
## Description

<!-- Describe your changes -->

Closes #

## Acceptance Criteria

- Keep the pull request focused.
- Include a short summary of the change.
- Link this issue in the pull request description. ✅

/bounty $50

DRAFT APPROACH (reference only — rewrite using actual repo code):
1. Setup your test environment  
Install dependencies:  
```bash
pip install pytest pytest-mock sqlite3
````


2. Create test file `tests/test_leaderboard.py` with these tests:  
```python
import pytest
from leaderboard import update_leaderboard
from unittest.mock import patch

# Mock database connection for tests
class SQLiteDatabase:
    def __init__(self):
        self.contributors = {}
    
    def add_contributor(self, name, score):
        self.contributors[name] = score
    
    def get_contributor(self, name):
        return self