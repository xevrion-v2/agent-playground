{
  "name": "@taskflow/pi",
  "version": "0.1.0",
  "private": true,
  "description": "Exact finite PI decimal prefix utilities for TaskFlow demos.",
  "type": "module",
  "main": "src/index.mjs",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "lint": "node --check src/index.mjs && node --check test/pi.test.mjs"
  }
}
