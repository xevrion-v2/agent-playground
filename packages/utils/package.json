{
  "name": "@taskflow/utils",
  "version": "0.1.0",
  "private": true,
  "description": "Shared utility functions for TaskFlow.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "node --experimental-vm-modules ../../node_modules/.bin/jest --config jest.config.js 2>/dev/null || node --test src/__tests__/sequence.test.mjs",
    "lint": "echo \"No utils lint configured yet\""
  }
}