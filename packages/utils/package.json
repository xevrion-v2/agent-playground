{
  "name": "@taskflow/utils",
  "version": "0.1.0",
  "private": true,
  "description": "Shared utility functions for TaskFlow.",
  "scripts": {
    "test": "vitest run",
    "lint": "echo \"No lint configured yet\""
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
