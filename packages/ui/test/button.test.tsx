import { Button } from "../src";

const enabledButton = <Button label="Save task" aria-label="Save task" />;
const disabledButton = <Button label="Delete task" disabled />;

void enabledButton;
void disabledButton;
