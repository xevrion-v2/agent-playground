# TaskFlow UI (`packages/ui`)

Shared UI components for the TaskFlow monorepo.

## Exports

- `Button` — stub component exported from `src/index.ts` (returns a plain object describing button props)

## Scripts

| Command | Description |
|---------|-------------|
| `npm run test` | Placeholder (no UI tests configured yet) |
| `npm run lint` | Placeholder (no UI lint configured yet) |

## Usage

```tsx
import { Button } from "@taskflow/ui";

const props = Button({ label: "Save", disabled: false });
```
