{
  "name": "@taskflow/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared UI components for TaskFlow.",
  "main": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit --module ESNext --moduleResolution bundler --target ES2022 --strict --skipLibCheck src/index.ts",
    "test": "echo \"No UI tests configured yet\"",
    "lint": "echo \"No UI lint configured yet\"",
    "build": "tsc --noEmit --module ESNext --moduleResolution bundler --target ES2022 --strict --skipLibCheck src/index.ts"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
