{
  "name": "@taskflow/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared UI components for TaskFlow.",
  "main": "src/index.ts",
  "scripts": {
    "test": "tsc src/index.ts src/index.test.ts --outDir .test-build --module NodeNext --moduleResolution NodeNext --target ES2022 && node --test .test-build/index.test.js",
    "lint": "echo \"No UI lint configured yet\""
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
