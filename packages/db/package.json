{
  "name": "@taskflow/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "schema:validate": "prisma validate",
    "schema:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  }
}
