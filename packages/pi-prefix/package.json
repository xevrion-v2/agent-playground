{
  "name": "@taskflow/pi-prefix",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic finite pi prefix calculator and verifier.",
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "taskflow-pi-prefix": "src/cli.js"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "lint": "node --check src/index.js && node --check src/cli.js"
  },
  "engines": {
    "node": ">=20"
  }
}
