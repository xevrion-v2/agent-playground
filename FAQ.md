# FAQ

## How do I add a new route?
1. Create a file in `apps/api/src/routes/`
2. Define GET/POST/PUT/DELETE handlers
3. Export default router
4. Add to `index.ts`

## How do I claim a bounty?
Comment `/attempt #N` on the issue, then submit a PR with `Closes #N`.
