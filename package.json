{
  "name": "taskflow-monorepo",
  "version": "1.0.0",
  "description": "TaskFlow Monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "workspaces": [
    "apps/*",
    "packages/*",
    "contributors/*",
    "node_modules/express-validator"
  ],
  "scripts": {
    "test": "vitest run",
    "node": ">=20"
  }
}
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "express-validator": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
