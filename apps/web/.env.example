# Environment variables for the Web app

# Node environment (development, production, test)
NODE_ENV=development

# Base URL of the API server (for client-side API calls)
NEXT_PUBLIC_API_URL=http://localhost:4000
