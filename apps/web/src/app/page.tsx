/**
 * TaskFlow Web — Home page.
 *
 * Required environment variables (passed at build time via Next.js):
 *   NEXT_PUBLIC_API_URL — Base URL for the API backend (default: http://localhost:4000)
 */

export default function HomePage() {
  return (
    <main>
      <h1>TaskFlow</h1>
      <p>Plan tasks, coordinate jobs, and manage proposals from one workspace.</p>
    </main>
  );
}
