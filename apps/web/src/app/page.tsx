import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "TaskFlow | Task and Proposal Workspace",
    description: "Plan tasks, coordinate jobs, and manage proposals from one TaskFlow workspace."
};

export default function HomePage() {
    return (
          <main>
                <h1>TaskFlow</h1>h1>
                <p>Plan tasks, coordinate jobs, and manage proposals from one workspace.</p>p>
          </main>main>
        );
}
</main>
