export default function ClientDashboardPage() {
  return (
    <main>
      <h1>Client Dashboard</h1>
      <p>Collect client-side task, proposal, and account workflows in one dashboard entry point.</p>
    </main>
  );
}