{
  "name": "api",
  "version": "1.0.0",
  "description": "TaskFlow API service with request body size limits",
  "scripts": {
    "dev": "ts-node-dev --no-notify --transpile-only src/index.ts",
    "build": "tsc",
  "description": "Express API service for TaskFlow.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx src/index.ts",
    "test": "echo \"No API tests configured yet\"",
    "lint": "echo \"No API lint configured yet\""
  },
  "dependencies": {
    "express": "^4.18.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsx": "^4.7.1",
    "typescript": "^5.4.5"
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0",
    "ts-node-dev": "^2.0.0"
  }
}
