/**
 * Returns whether the input string contains the lowercase 's' character.
 * @param value - The string to check.
 * @returns true if the string contains at least one 's', false otherwise.
 */
export function hasLowercaseSCharacter(value: string): boolean {
  return value.includes('s');
}
