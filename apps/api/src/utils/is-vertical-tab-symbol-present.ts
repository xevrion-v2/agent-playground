/**
 * Returns whether `value` contains the vertical-tab symbol.
 * Dependency-free utility for API symbol checks.
 */
export function isVerticalTabSymbolPresent(value: string): boolean {
  return value.includes("");
}
