/**
 * Checks if a string contains the lowercase letter 't'.
 * @param value - The string to check.
 * @returns True if the string contains at least one 't', false otherwise.
 */
export function hasLowercaseTCharacter(value: string): boolean {
  return value.includes('t');
}
